{{systemPrompt}}

Context information (JSON):

JSON Structure:
- linesBefore (array, optional): Up to 5 lines before the cursor position
- currentText (string, required): Text from the start of current line to cursor position, or selected text to complete
- linesAfter (array, optional): Up to 5 lines after the cursor position

JSON Data:
{{contextJson}}

Complete the paragraph starting from the currentText. Generate a full, well-structured paragraph that naturally continues and concludes the thought. Do not repeat the currentText itself in the output. Generate a complete paragraph (3-5 sentences) that maintains the same tone and style.

